# Этап 13: итоговая проверка

Дата: 2026-09-10. Исходная версия — `7f4f2f4`. Проверка продолжается.

## Находки

- Исправлен тест каталога MSBuild-диагностик: после переноса справки он продолжал читать `generated-code.md`. Исправление опубликовано в `90923c8`.
- **S13-01, блокирует релиз:** наследуемые `Construct`, `Members` и `Convert`, читающие публичные именованные поля кортежа (`source.Data.Value`, `source.Data.Count`), получают ложный `MORPH0028`. Воспроизводится и с generic-подстановкой, и с уже закрытым типом `string`. Пример использует `base.Configure` и точный `IncludeBase`; закрытых членов и interface dispatch в нём нет. Постоянное воспроизведение: `InheritanceDiagnosticsTests/TupleMemberBindingTests.cs`.

Первоначальный runtime-сценарий перенесён в unit-проверку компиляции, чтобы ошибка генерации не мешала запуску остальных integration-тестов. Ожидание отсутствия диагностик сохранено: воспроизводящие тесты пока красные.

Вложенный полиморфный Update проверяется девятью новыми runtime-тестами: выбор Create при отсутствии внешнего destination, сохранение объектов, null-source/null-destination, несовместимый subtype и вложенное значение после замены внешнего объекта. Итоговые результаты ещё не получены.

Исправление S13-01 требует отдельного согласования согласно порядку релизного аудита. Production-код на этом этапе пока не менялся.
