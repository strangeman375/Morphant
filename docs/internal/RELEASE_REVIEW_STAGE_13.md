# Этап 13: итоговая проверка

Дата: 2026-09-10. **Этап завершён, S13-01 закрыт.**

Код и тесты проверены на `e487fa4`: [полный CI](https://github.com/strangeman375/Morphant/actions/runs/34469584557) прошёл. Подтверждённых блокирующих дефектов в проверенной области не осталось.

## Исправления

- **S13-01:** наследуемые `Construct`, `Members` и `Convert` корректно читают поля кортежа после generic-подстановки. Исправлены также тип повторно используемого выражения, подстановка длинного кортежа и условный доступ к nullable-элементу. Проверки недоступных членов и семантики struct-обращений сохранены.
- Исправлен тест каталога MSBuild-диагностик: после переноса справки он проверяет `build-diagnostics.md`.

Постоянные проверки: [21 случай компиляции](../../src/tests/Morphant.Generator.UnitTests/InheritanceDiagnosticsTests/TupleMemberBindingTests.cs) и [runtime-сценарии](../../src/tests/Morphant.Generator.IntegrationTests.CSharp9/Scenarios/TupleGenericInheritance/Scenario.cs) с `IncludeBase`, переопределением `Members`, Create/Update, именованными и позиционными полями, вложенным nullable-элементом длинного кортежа.

## Проверка

Release-сборки локально и в CI на трёх ОС: 0 warnings, 0 errors. Локально дополнительно прошли 31 целевая unit-проверка и 109 runtime-проверок кортежей и наследования.

| Проверка CI | Passed / Failed / Skipped |
| --- | --- |
| Unit, Ubuntu, Roslyn 4.4 | 1553 / 0 / 1 |
| Unit, Windows, Roslyn 4.4 | 1534 / 0 / 20 |
| Unit, macOS, Roslyn 4.4 | 1552 / 0 / 2 |
| Неизменённый generator 4.4 на host 4.9.2, Ubuntu | 1554 / 0 / 0 |
| Integration, Ubuntu и macOS, на каждой ОС | 530 / 0 / 0 |
| Integration, Windows | 527 / 0 / 3 |

Package consumer на SDK 7.0.100 / MSBuild 17.4 прошёл. Проверены итоговые `.nupkg`/`.snupkg`, состав пакета, strong name и реальные сборки потребителей; локально подтверждено совпадение упакованных бинарных файлов с Release-сборкой. Все пороги покрытия пройдены; generator: 88,05% строк / 78,63% ветвей.

Пропуски обусловлены версией компилятора и платформой: collection expressions на Roslyn 4.4, файловые ссылки, Unix permissions и недопустимые имена путей на Windows, Unicode-нормализация файловой системы на macOS.

## Область проверки

Сверены F01–F26 с результатами этапов и текущими тестами. Проверены независимость expectations, compiler warnings, значения, identity, исключения и сохранность файлов. Все девять добавленных сочетаний вложенного полиморфного Update, null и замены результата проходят.

Q01–Q06 закрыты в проверенной области этапов 3, 9–11; Q08 учтён через полные поверхности, обязательность входов, изоляцию и переиспользование. Q07 — выбор версии и metadata релиза — остаётся отдельным шагом выпуска. Исчерпывающая комбинаторика, реальные IDE и все .NET Standard-совместимые runtime не проверены.
