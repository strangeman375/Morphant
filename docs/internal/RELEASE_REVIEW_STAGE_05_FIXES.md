# Исправления этапа 5

Дата начала: 2026-09-07. Пользователь согласовал выводы
[аудита этапа 5](RELEASE_REVIEW_STAGE_05.md), разрешил исправления,
плотное покрытие тестами и публикацию контрольных точек в `main`.
Исходная контрольная точка: `593ac4c`; код продукта проверен аудитом
на `3352e94`. Этап 6 в эту работу не входит.

Статус: S05-02 исправлен. Для S05-01 реализовано и проверено
[единое значение](RELEASE_REVIEW_STAGE_05_MEMBER_VALUES.md) параметра
конструктора и члена. Остаётся обсуждение result-зависимого значения.
Результаты ниже относятся к предыдущей реализации. Проверена версия
[`ba151fb`](https://github.com/strangeman375/Morphant/commit/ba151fb68c0e9ffa482988fbbddabe9a792a1d6f).
Итоговые значения и диагностики сохранены в
[fix-results.json](release-review-stage-05/fix-results.json).

## Исходное решение (S05-01 впоследствии пересмотрен)

S05-01: явно заданное правило `Members` должно применяться и тогда,
когда конструктор, выбранный автоматически или через `ByConvention`,
имеет одноимённый параметр. Исключать повторное назначение можно для
автоматического правила. Явное выражение нельзя объединять с другим
вычислением только из-за совпадения имён.

S05-02: перенос callback из базового маппера должен сохранять исходное
связывание невиртуальных членов и обычную виртуальную диспетчеризацию.
Если эквивалентное обращение недоступно в производном маппере, требуется
диагностика и типизированное восстановление вместо молчаливого выбора
другого члена. Спорные изменения API или границ поддержки обсуждаются
с пользователем до реализации.

## Контрольные точки и критерии завершения

| Контрольная точка | Проверки |
| --- | --- |
| S05-01 | Автоматический конструктор, `ByConvention` и явный конструктор; `Create`, `Update(null)`, `Update(existing)`; set/init/required, `SetsRequiredMembers`; ветви, явные выражения и число вычислений; обычные conventions без лишних назначений |
| S05-02 | Все шесть семейств callbacks; простое и явное `this`-обращение, method groups, deferred expressions; скрытие и перегрузки, свойства и поля; несколько уровней и generic-подстановки; static и virtual-контроли |
| Безопасный отказ | Недоступное эквивалентное связывание, точная диагностика, типизированные заглушки и независимая корректная пара; актуализация после изменения исходника |
| Итог | Полные Release, unit и integration; затронутые категории Roslyn 4.4.0/4.9.2; повтор исходных MSBuild-воспроизведений; актуальные краткие документы и результаты |

Compiler-регрессии проверяют observable output и диагностики генератора;
runtime-регрессии компилируются MSBuild как обычные consumers.
Каждая завершённая контрольная точка публикуется до следующей.

## Результаты

S05-01: фильтр совпадающих constructor/member-правил сохраняет явное
выражение до исключения автоматических назначений и объединения required
conventions. Добавлены 29 runtime и 12 compiler-сценариев.
Направленные прогоны: 23 unit и 59 integration passed, без пропусков и ошибок.

Первый расширенный runtime-прогон: 42 passed, четыре неверных ожидания числа
чтений в новом контроле явного `Construct`. Исправлено ожидание: одинаковая
зависимость `source.Value` двух явных DSL callbacks переиспользуется один раз,
как в существующем `SharedValuesTests`. Значения назначения были правильными.
Повтор с исправленными ожиданиями входит в 59 успешных integration-тестов.

S05-02: при скрытии выбирается обращение через исходный объявляющий тип
или `base`, когда это сохраняет доступность и диспетчеризацию. Обычный
override остаётся виртуальным. Защищённый член, к которому нельзя обратиться
таким способом без изменения выбранной группы членов, даёт MORPH0028.
Этот же выбор используется в проверке доступности и в переносе выражения.
Явный `base` в импортируемом пользовательском callback по-прежнему запрещён.

Первые 15 runtime-тестов S05-02 прошли: шесть семейств с неявным/явным `this`,
Create/Update, generic-подстановка через промежуточную базу, поля и свойства,
перегрузки, static/virtual, method groups, delegate getter и deferred callback.
Все шесть compiler-проверок безопасности и актуализации прошли; первый
прогон потребовал уточнить только ожидание порядка диагностик в новом тесте.
Дополнительные три runtime-воспроизведения обнаружили смену перегрузки
после covariant override, переноса самостоятельного `this` и закрытия
generic-семейства. Перенос теперь сохраняет исходный тип результата и
`this`, а изменившиеся при подстановке выведенные generic-аргументы
указывает явно. Все 18 runtime-тестов наследования прошли; семь новых
compiler-тестов проверяют символы выбранных методов в готовой compilation.
Направленный unit-прогон: 39 passed, без предупреждений и ошибок.

Добавлен MSBuild consumer для подавленного MORPH0028: все шесть семейств
и транзитивная пара должны выбрасывать MappingConfigurationException на
Create/Update(null)/Update(existing) с правильной операцией и типами;
независимая корректная пара должна выполняться. Этот consumer прошёл;
направленный integration-прогон наследования — 40 passed. Полная
Release-сборка прошла без предупреждений и ошибок.

## Итоговая проверка

Добавлены 73 постоянных регрессии: 25 compiler-тестов и 48 runtime-тестов.

| Проверка | Результат |
| --- | --- |
| Полная Release-сборка solution | 0 warnings, 0 errors |
| Полный unit, Roslyn 4.4.0 | 871 passed, один штатный skip для C# 12 collection expressions |
| Полный integration, SDK 10.0.100 | 319 passed, без skips и failures |
| Затронутые unit-категории, Roslyn 4.9.2 | 197 passed, без skips и failures |
| CI: Linux, Windows и macOS | Release, полные unit и integration прошли |
| CI: Roslyn 4.9.2 и package consumer SDK 7.0.100 | Прошли |
| Исходные MSBuild-воспроизведения | Все 15 конфигураций дали ожидаемый результат; все 83 runtime-проверки успешны |

[CI проверенной версии](https://github.com/strangeman375/Morphant/actions/runs/34140518809)
завершился успешно, включая установленные пороги покрытия.
После проверки Roslyn 4.9.2 локальные зависимости возвращены к 4.4.0;
unit-проект повторно собран в Release без предупреждений и ошибок.

Исходный полный локальный integration-прогон завершился за 5 минут 45 секунд.
Во время ожидания результата был запущен дополнительный прогон с ограничением
бездействия 60 секунд. Он успел пройти 58 тестов и был остановлен на
`PackageConsumptionTests.Packs_complete_assets_and_applies_buildTransitive_contract`.
Этот неполный повтор не засчитывается как полный результат; полный прогон
и CI той же версии прошли.

Воспроизведения сохранены с исходными ожиданиями. Девять положительных
вариантов собираются без диагностик; шесть отрицательных сохраняют
MORPH0018, MORPH0028, MORPH0030, MORPH0031 и MORPH0032 согласно каждому
входу. Исторические 12 расхождений в `results.json` остаются результатами
версии до исправлений.

Обновлены страницы Members, наследования и MORPH0028, индекс диагностик,
changelog, план и отчёт аудита. Этап 6 не начинался.
