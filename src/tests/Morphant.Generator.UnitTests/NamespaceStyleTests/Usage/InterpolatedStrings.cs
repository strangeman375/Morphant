namespace Morphant.Generator.UnitTests.NamespaceStyleTests.Usage;

internal sealed partial class InterpolatedStringsTests
{
    // lang=c#
    private const string VerbatimSource =
"""
#nullable enable
#pragma warning disable CS1591
using Morphant;

namespace Example
{
    public sealed class Source
    {
        public string Name { get; set; } = string.Empty;
    }

    public sealed class Destination
    {
        public Destination(string value) => Value = value;
        public string Value { get; set; }
    }

    [MorphantMapper]
    public partial class Mapper : TypeMapper<Mapper>
    {
        protected override void Configure(MapperBuilder builder) =>
            builder.Map<Source, Destination>()
                .Construct(source => new($@"construct {source.Name}
  ""quoted"" {{braces}}

last"))
                .Members(source => new()
                {
                    Value = $@"member {source.Name}
  ""quoted"" {{braces}}

last"
                });
    }
}
""";

    // lang=c#
    private const string RawSource =
""""
#nullable enable
#pragma warning disable CS1591
using Morphant;

namespace Example
{
    public sealed class Source
    {
        public string Name { get; set; } = string.Empty;
    }

    public sealed class Destination
    {
        public Destination(string value) => Value = value;
        public string Value { get; set; }
    }

    [MorphantMapper]
    public partial class Mapper : TypeMapper<Mapper>
    {
        protected override void Configure(MapperBuilder builder) =>
            builder.Map<Source, Destination>()
                .Construct(source => new($$"""
                    construct {{source.Name}}
                      "quoted" {braces}

                    last
                    """))
                .Members(source => new()
                {
                    Value = $$"""
                        member {{source.Name}}
                          "quoted" {braces}

                        last
                        """
                });
    }
}
"""";

    // lang=c#
    private const string VerbatimBlockMapper =
"""
// <auto-generated />
#nullable enable

namespace Example
{
    public partial class Mapper :
        global::Morphant.ITypeMapper<global::Example.Source, global::Example.Destination>
    {
        /// <inheritdoc/>
        protected override bool Supports(
            global::System.Type sourceType,
            global::System.Type destinationType) =>
                (sourceType == typeof(global::Example.Source) &&
                    destinationType == typeof(global::Example.Destination)) ||
                base.Supports(sourceType, destinationType);

        /// <inheritdoc/>
        global::Example.Destination global::Morphant.ITypeMapper<global::Example.Source, global::Example.Destination>.Create(
            global::Example.Source? source,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            return __Create(source);
        }

        /// <inheritdoc/>
        global::Example.Destination global::Morphant.ITypeMapper<global::Example.Source, global::Example.Destination>.Update(
            global::Example.Source? source,
            global::Example.Destination? destination,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (destination is null)
            {
                return __Create(source);
            }

            return __Update(source, destination);
        }

        private global::Example.Destination __Create(
            global::Example.Source source)
        {
            return new global::Example.Destination(
                value: $@"construct {source.Name}
  ""quoted"" {{braces}}

last")
            {
                Value = $@"member {source.Name}
  ""quoted"" {{braces}}

last"
            };
        }

        private global::Example.Destination __Update(
            global::Example.Source source,
            global::Example.Destination destination)
        {
            destination.Value = $@"member {source.Name}
  ""quoted"" {{braces}}

last";

            return destination;
        }
    }
}
""";

    // lang=c#
    private const string VerbatimFileMapper =
"""
// <auto-generated />
#nullable enable

namespace Example;

public partial class Mapper :
    global::Morphant.ITypeMapper<global::Example.Source, global::Example.Destination>
{
    /// <inheritdoc/>
    protected override bool Supports(
        global::System.Type sourceType,
        global::System.Type destinationType) =>
            (sourceType == typeof(global::Example.Source) &&
                destinationType == typeof(global::Example.Destination)) ||
            base.Supports(sourceType, destinationType);

    /// <inheritdoc/>
    global::Example.Destination global::Morphant.ITypeMapper<global::Example.Source, global::Example.Destination>.Create(
        global::Example.Source? source,
        global::Morphant.Context.MappingContext context)
    {
        if (source is null)
        {
            return default!;
        }

        return __Create(source);
    }

    /// <inheritdoc/>
    global::Example.Destination global::Morphant.ITypeMapper<global::Example.Source, global::Example.Destination>.Update(
        global::Example.Source? source,
        global::Example.Destination? destination,
        global::Morphant.Context.MappingContext context)
    {
        if (source is null)
        {
            return default!;
        }

        if (destination is null)
        {
            return __Create(source);
        }

        return __Update(source, destination);
    }

    private global::Example.Destination __Create(
        global::Example.Source source)
    {
        return new global::Example.Destination(
            value: $@"construct {source.Name}
  ""quoted"" {{braces}}

last")
        {
            Value = $@"member {source.Name}
  ""quoted"" {{braces}}

last"
        };
    }

    private global::Example.Destination __Update(
        global::Example.Source source,
        global::Example.Destination destination)
    {
        destination.Value = $@"member {source.Name}
  ""quoted"" {{braces}}

last";

        return destination;
    }
}
""";

    // lang=c#
    private const string RawFileMapper =
""""
// <auto-generated />
#nullable enable

namespace Example;

public partial class Mapper :
    global::Morphant.ITypeMapper<global::Example.Source, global::Example.Destination>
{
    /// <inheritdoc/>
    protected override bool Supports(
        global::System.Type sourceType,
        global::System.Type destinationType) =>
            (sourceType == typeof(global::Example.Source) &&
                destinationType == typeof(global::Example.Destination)) ||
            base.Supports(sourceType, destinationType);

    /// <inheritdoc/>
    global::Example.Destination global::Morphant.ITypeMapper<global::Example.Source, global::Example.Destination>.Create(
        global::Example.Source? source,
        global::Morphant.Context.MappingContext context)
    {
        if (source is null)
        {
            return default!;
        }

        return __Create(source);
    }

    /// <inheritdoc/>
    global::Example.Destination global::Morphant.ITypeMapper<global::Example.Source, global::Example.Destination>.Update(
        global::Example.Source? source,
        global::Example.Destination? destination,
        global::Morphant.Context.MappingContext context)
    {
        if (source is null)
        {
            return default!;
        }

        if (destination is null)
        {
            return __Create(source);
        }

        return __Update(source, destination);
    }

    private global::Example.Destination __Create(
        global::Example.Source source)
    {
        return new global::Example.Destination(
            value: $$"""
                    construct {{source.Name}}
                      "quoted" {braces}

                    last
                    """)
        {
            Value = $$"""
                        member {{source.Name}}
                          "quoted" {braces}

                        last
                        """
        };
    }

    private global::Example.Destination __Update(
        global::Example.Source source,
        global::Example.Destination destination)
    {
        destination.Value = $$"""
                        member {{source.Name}}
                          "quoted" {braces}

                        last
                        """;

        return destination;
    }
}
"""";

}
