namespace Morphant.Generator.UnitTests.QueryVariableNamesTests.Usage;

internal sealed partial class QueryVariableNamesTests
{
    // lang=c#
    private const string ShadowedHelperSource =
"""
#nullable enable
#pragma warning disable CS1591
using System.Linq;
using Morphant;
namespace QueryNames
{
    public sealed class Source
    {
        public int[] Values { get; set; } = new int[0];
    }
    public sealed class Destination
    {
        public Destination(string value) => Value = value;
        public string Value { get; set; }
    }
    [MorphantMapper]
    public partial class Mapper : TypeMapper<Mapper>
    {
        protected override void Configure(MapperBuilder builder) =>
            builder.Map<Source, Destination>()
                .Resolve((input, previous) =>
                {
                    if (previous.HasValue)
                    {
                        global::System.Func<Source, global::Morphant.Context.MappingContext, Destination> __Create =
                            (value, context) => new Destination("custom");
                        if (input.Values.Length == 0 && __Create(input, default).Value == "custom")
                            return previous.Value;
                        return new(input.Values[0].ToString());
                    }
                    return new(input.Values[0].ToString());
                });
    }
}
""";

    // lang=c#
    private const string ShadowedHelperMapper =
"""
// <auto-generated />
#nullable enable

namespace QueryNames
{
    public partial class Mapper :
        global::Morphant.ITypeMapper<global::QueryNames.Source, global::QueryNames.Destination>
    {
        /// <inheritdoc/>
        protected override bool Supports(
            global::System.Type sourceType,
            global::System.Type destinationType) =>
                (sourceType == typeof(global::QueryNames.Source) &&
                    destinationType == typeof(global::QueryNames.Destination)) ||
                base.Supports(sourceType, destinationType);

        /// <inheritdoc/>
        global::QueryNames.Destination global::Morphant.ITypeMapper<global::QueryNames.Source, global::QueryNames.Destination>.Create(
            global::QueryNames.Source? source,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            return __Create(source);
        }

        /// <inheritdoc/>
        global::QueryNames.Destination global::Morphant.ITypeMapper<global::QueryNames.Source, global::QueryNames.Destination>.Update(
            global::QueryNames.Source? source,
            global::QueryNames.Destination? destination,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (destination is null)
            {
                return __Create(source);
            }

            return __Update(source, destination);
        }

        private global::QueryNames.Destination __Create(
            global::QueryNames.Source source)
        {
            return new global::QueryNames.Destination(
                value: source.Values[0].ToString());
        }

        private global::QueryNames.Destination __Update(
            global::QueryNames.Source source,
            global::QueryNames.Destination destination)
        {
            global::System.Func<global::QueryNames.Source, global::Morphant.Context.MappingContext, global::QueryNames.Destination> __Create = (value, context) => new global::QueryNames.Destination("custom");

            if (source.Values.Length == 0 && __Create(source, default).Value == "custom")
            {
                return destination;
            }

            return this.__Create(source);
        }
    }
}
""";
}
