using Microsoft.CodeAnalysis.CSharp;
using Morphant.Generator.UnitTests.TestUtils;

namespace Morphant.Generator.UnitTests.RuntimePolymorphismTests;

[TestFixture]
internal sealed class DispatchTests
{
    [Test]
    public async Task Emits_compact_switch_dispatch_for_class_branches()
    {
        // lang=c#
        const string source =
"""
#nullable enable
#pragma warning disable CS1591

using Morphant;

namespace TestCase
{
    public class Animal { }
    public sealed class Dog : Animal { }
    public sealed class Cat : Animal { }
    public class AnimalDto { }
    public sealed class DogDto : AnimalDto { }
    public sealed class CatDto : AnimalDto { }

    [MorphantMapper]
    public partial class TestMapper : TypeMapper<TestMapper>
    {
        protected override void Configure(MapperBuilder builder)
        {
            builder.Map<Animal, AnimalDto>()
                .ForDerived<Dog, DogDto>()
                .ForDerived<Cat, CatDto>();
            builder.Map<Dog, DogDto>();
            builder.Map<Cat, CatDto>();
        }
    }
}
""";

        // lang=c#
        const string expected =
"""
// <auto-generated />
#nullable enable

namespace TestCase
{
    public partial class TestMapper :
        global::Morphant.ITypeMapper<global::TestCase.Animal, global::TestCase.AnimalDto>,
        global::Morphant.ITypeMapper<global::TestCase.Dog, global::TestCase.DogDto>,
        global::Morphant.ITypeMapper<global::TestCase.Cat, global::TestCase.CatDto>
    {
        /// <inheritdoc/>
        protected override bool Supports(
            global::System.Type sourceType,
            global::System.Type destinationType) =>
                (sourceType == typeof(global::TestCase.Animal) &&
                    destinationType == typeof(global::TestCase.AnimalDto)) ||
                (sourceType == typeof(global::TestCase.Dog) &&
                    destinationType == typeof(global::TestCase.DogDto)) ||
                (sourceType == typeof(global::TestCase.Cat) &&
                    destinationType == typeof(global::TestCase.CatDto)) ||
                base.Supports(sourceType, destinationType);

        /// <inheritdoc/>
        global::TestCase.AnimalDto global::Morphant.ITypeMapper<global::TestCase.Animal, global::TestCase.AnimalDto>.Create(
            global::TestCase.Animal? source,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (__TryCreatePolymorphic(source, context, out var polymorphicDestination))
            {
                return polymorphicDestination;
            }

            return __Create(source, context);
        }

        /// <inheritdoc/>
        global::TestCase.AnimalDto global::Morphant.ITypeMapper<global::TestCase.Animal, global::TestCase.AnimalDto>.Update(
            global::TestCase.Animal? source,
            global::TestCase.AnimalDto? destination,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (__TryUpdatePolymorphic(source, destination, context, out var polymorphicDestination))
            {
                return polymorphicDestination;
            }

            if (destination is null)
            {
                return __Create(source, context);
            }

            return __Update(source, destination, context);
        }

        private bool __TryCreatePolymorphic(
            global::TestCase.Animal source,
            global::Morphant.Context.MappingContext context,
            out global::TestCase.AnimalDto result)
        {
            switch (source)
            {
                case global::TestCase.Dog polymorphicSource:
                    result = context.Mapper.Map<global::TestCase.Dog, global::TestCase.DogDto>(polymorphicSource);
                    return true;

                case global::TestCase.Cat polymorphicSource:
                    result = context.Mapper.Map<global::TestCase.Cat, global::TestCase.CatDto>(polymorphicSource);
                    return true;

                default:
                    result = default!;
                    return false;
            }
        }

        private bool __TryUpdatePolymorphic(
            global::TestCase.Animal source,
            global::TestCase.AnimalDto? destination,
            global::Morphant.Context.MappingContext context,
            out global::TestCase.AnimalDto result)
        {
            switch (source)
            {
                case global::TestCase.Dog polymorphicSource:
                    result = global::Morphant.RuntimeSupport.MappingHelpers.UpdateDerived<
                        global::TestCase.Animal,
                        global::TestCase.AnimalDto,
                        global::TestCase.Dog,
                        global::TestCase.DogDto>(
                        polymorphicSource,
                        destination,
                        context);
                    return true;

                case global::TestCase.Cat polymorphicSource:
                    result = global::Morphant.RuntimeSupport.MappingHelpers.UpdateDerived<
                        global::TestCase.Animal,
                        global::TestCase.AnimalDto,
                        global::TestCase.Cat,
                        global::TestCase.CatDto>(
                        polymorphicSource,
                        destination,
                        context);
                    return true;

                default:
                    result = default!;
                    return false;
            }
        }

        private global::TestCase.AnimalDto __Create()
        {
            return new global::TestCase.AnimalDto();
        }

        private global::TestCase.AnimalDto __Update(
            global::TestCase.AnimalDto destination)
        {
            return destination;
        }

        /// <inheritdoc/>
        global::TestCase.DogDto global::Morphant.ITypeMapper<global::TestCase.Dog, global::TestCase.DogDto>.Create(
            global::TestCase.Dog? source,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            return __Create1(source, context);
        }

        /// <inheritdoc/>
        global::TestCase.DogDto global::Morphant.ITypeMapper<global::TestCase.Dog, global::TestCase.DogDto>.Update(
            global::TestCase.Dog? source,
            global::TestCase.DogDto? destination,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (destination is null)
            {
                return __Create1(source, context);
            }

            return __Update1(source, destination, context);
        }

        private global::TestCase.DogDto __Create1()
        {
            return new global::TestCase.DogDto();
        }

        private global::TestCase.DogDto __Update1(
            global::TestCase.DogDto destination)
        {
            return destination;
        }

        /// <inheritdoc/>
        global::TestCase.CatDto global::Morphant.ITypeMapper<global::TestCase.Cat, global::TestCase.CatDto>.Create(
            global::TestCase.Cat? source,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            return __Create2(source, context);
        }

        /// <inheritdoc/>
        global::TestCase.CatDto global::Morphant.ITypeMapper<global::TestCase.Cat, global::TestCase.CatDto>.Update(
            global::TestCase.Cat? source,
            global::TestCase.CatDto? destination,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (destination is null)
            {
                return __Create2(source, context);
            }

            return __Update2(source, destination, context);
        }

        private global::TestCase.CatDto __Create2()
        {
            return new global::TestCase.CatDto();
        }

        private global::TestCase.CatDto __Update2(
            global::TestCase.CatDto destination)
        {
            return destination;
        }
    }
}
""";

        await ConventionTypeMapperGeneratorTest.RunAndAssert(
            LanguageVersion.CSharp9,
            source,
            expected);
    }

    [Test]
    public async Task Emits_strict_unknown_type_guard_before_base_fallback()
    {
        // lang=c#
        const string source =
"""
#nullable enable
#pragma warning disable CS1591

using Morphant;

namespace TestCase
{
    public class Animal { }
    public sealed class Dog : Animal { }
    public class AnimalDto { }
    public sealed class DogDto : AnimalDto { }

    [MorphantMapper]
    public partial class TestMapper : TypeMapper<TestMapper>
    {
        protected override void Configure(MapperBuilder builder)
        {
            builder.Map<Animal, AnimalDto>()
                .ForDerived<Dog, DogDto>()
                .UnknownDerivedTypeHandling(
                    UnknownDerivedTypeHandling.Throw);
        }
    }
}
""";

        // lang=c#
        const string expected =
"""
// <auto-generated />
#nullable enable

namespace TestCase
{
    public partial class TestMapper :
        global::Morphant.ITypeMapper<global::TestCase.Animal, global::TestCase.AnimalDto>
    {
        /// <inheritdoc/>
        protected override bool Supports(
            global::System.Type sourceType,
            global::System.Type destinationType) =>
                (sourceType == typeof(global::TestCase.Animal) &&
                    destinationType == typeof(global::TestCase.AnimalDto)) ||
                base.Supports(sourceType, destinationType);

        /// <inheritdoc/>
        global::TestCase.AnimalDto global::Morphant.ITypeMapper<global::TestCase.Animal, global::TestCase.AnimalDto>.Create(
            global::TestCase.Animal? source,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (__TryCreatePolymorphic(source, context, out var polymorphicDestination))
            {
                return polymorphicDestination;
            }

            return __Create(source, context);
        }

        /// <inheritdoc/>
        global::TestCase.AnimalDto global::Morphant.ITypeMapper<global::TestCase.Animal, global::TestCase.AnimalDto>.Update(
            global::TestCase.Animal? source,
            global::TestCase.AnimalDto? destination,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (__TryUpdatePolymorphic(source, destination, context, out var polymorphicDestination))
            {
                return polymorphicDestination;
            }

            if (destination is null)
            {
                return __Create(source, context);
            }

            return __Update(source, destination, context);
        }

        private bool __TryCreatePolymorphic(
            global::TestCase.Animal source,
            global::Morphant.Context.MappingContext context,
            out global::TestCase.AnimalDto result)
        {
            switch (source)
            {
                case global::TestCase.Dog polymorphicSource:
                    result = context.Mapper.Map<global::TestCase.Dog, global::TestCase.DogDto>(polymorphicSource);
                    return true;

                case { } when source.GetType() !=
                    typeof(global::TestCase.Animal):
                    throw global::Morphant.Exceptions.UnmatchedPolymorphicMappingException.Create<
                        global::TestCase.Animal,
                        global::TestCase.AnimalDto>(
                        global::Morphant.Context.MappingOperation.Create,
                        source);

                default:
                    result = default!;
                    return false;
            }
        }

        private bool __TryUpdatePolymorphic(
            global::TestCase.Animal source,
            global::TestCase.AnimalDto? destination,
            global::Morphant.Context.MappingContext context,
            out global::TestCase.AnimalDto result)
        {
            switch (source)
            {
                case global::TestCase.Dog polymorphicSource:
                    result = global::Morphant.RuntimeSupport.MappingHelpers.UpdateDerived<
                        global::TestCase.Animal,
                        global::TestCase.AnimalDto,
                        global::TestCase.Dog,
                        global::TestCase.DogDto>(
                        polymorphicSource,
                        destination,
                        context);
                    return true;

                case { } when source.GetType() !=
                    typeof(global::TestCase.Animal):
                    throw global::Morphant.Exceptions.UnmatchedPolymorphicMappingException.Create<
                        global::TestCase.Animal,
                        global::TestCase.AnimalDto>(
                        global::Morphant.Context.MappingOperation.Update,
                        source);

                default:
                    result = default!;
                    return false;
            }
        }

        private global::TestCase.AnimalDto __Create()
        {
            return new global::TestCase.AnimalDto();
        }

        private global::TestCase.AnimalDto __Update(
            global::TestCase.AnimalDto destination)
        {
            return destination;
        }
    }
}
""";

        await ConventionTypeMapperGeneratorTest.RunAndAssert(
            LanguageVersion.CSharp9,
            source,
            expected);
    }

    [Test]
    public async Task Emits_explicit_ambiguity_path_for_overlapping_interfaces()
    {
        // lang=c#
        const string source =
"""
#nullable enable
#pragma warning disable CS1591

using Morphant;

namespace TestCase
{
    public interface IRoot { }
    public interface IWorking : IRoot { }
    public interface IPet : IRoot { }
    public class RootDto { }
    public sealed class WorkingDto : RootDto { }
    public sealed class PetDto : RootDto { }

    [MorphantMapper]
    public partial class TestMapper : TypeMapper<TestMapper>
    {
        protected override void Configure(MapperBuilder builder)
        {
            builder.Map<IRoot, RootDto>()
                .ForDerived<IWorking, WorkingDto>()
                .ForDerived<IPet, PetDto>();
        }
    }
}
""";

        // lang=c#
        const string expected =
"""
// <auto-generated />
#nullable enable

namespace TestCase
{
    public partial class TestMapper :
        global::Morphant.ITypeMapper<global::TestCase.IRoot, global::TestCase.RootDto>
    {
        /// <inheritdoc/>
        protected override bool Supports(
            global::System.Type sourceType,
            global::System.Type destinationType) =>
                (sourceType == typeof(global::TestCase.IRoot) &&
                    destinationType == typeof(global::TestCase.RootDto)) ||
                base.Supports(sourceType, destinationType);

        /// <inheritdoc/>
        global::TestCase.RootDto global::Morphant.ITypeMapper<global::TestCase.IRoot, global::TestCase.RootDto>.Create(
            global::TestCase.IRoot? source,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (__TryCreatePolymorphic(source, context, out var polymorphicDestination))
            {
                return polymorphicDestination;
            }

            return __Create(source, context);
        }

        /// <inheritdoc/>
        global::TestCase.RootDto global::Morphant.ITypeMapper<global::TestCase.IRoot, global::TestCase.RootDto>.Update(
            global::TestCase.IRoot? source,
            global::TestCase.RootDto? destination,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (__TryUpdatePolymorphic(source, destination, context, out var polymorphicDestination))
            {
                return polymorphicDestination;
            }

            if (destination is null)
            {
                return __Create(source, context);
            }

            return __Update(source, destination, context);
        }

        private bool __TryCreatePolymorphic(
            global::TestCase.IRoot source,
            global::Morphant.Context.MappingContext context,
            out global::TestCase.RootDto result)
        {
            switch (source)
            {
                case global::TestCase.IWorking polymorphicSource
                    when source is not global::TestCase.IPet:
                    result = context.Mapper.Map<global::TestCase.IWorking, global::TestCase.WorkingDto>(polymorphicSource);
                    return true;

                case global::TestCase.IPet polymorphicSource
                    when source is not global::TestCase.IWorking:
                    result = context.Mapper.Map<global::TestCase.IPet, global::TestCase.PetDto>(polymorphicSource);
                    return true;

                case { } when source is global::TestCase.IWorking ||
                    source is global::TestCase.IPet:
                    throw global::Morphant.Exceptions.AmbiguousPolymorphicMappingException.Create<
                        global::TestCase.IRoot,
                        global::TestCase.RootDto>(
                        global::Morphant.Context.MappingOperation.Create,
                        source,
                        (
                            source is global::TestCase.IWorking,
                            typeof(global::TestCase.IWorking),
                            typeof(global::TestCase.WorkingDto)),
                        (
                            source is global::TestCase.IPet,
                            typeof(global::TestCase.IPet),
                            typeof(global::TestCase.PetDto)));

                default:
                    result = default!;
                    return false;
            }
        }

        private bool __TryUpdatePolymorphic(
            global::TestCase.IRoot source,
            global::TestCase.RootDto? destination,
            global::Morphant.Context.MappingContext context,
            out global::TestCase.RootDto result)
        {
            switch (source)
            {
                case global::TestCase.IWorking polymorphicSource
                    when source is not global::TestCase.IPet:
                    result = global::Morphant.RuntimeSupport.MappingHelpers.UpdateDerived<
                        global::TestCase.IRoot,
                        global::TestCase.RootDto,
                        global::TestCase.IWorking,
                        global::TestCase.WorkingDto>(
                        polymorphicSource,
                        destination,
                        context);
                    return true;

                case global::TestCase.IPet polymorphicSource
                    when source is not global::TestCase.IWorking:
                    result = global::Morphant.RuntimeSupport.MappingHelpers.UpdateDerived<
                        global::TestCase.IRoot,
                        global::TestCase.RootDto,
                        global::TestCase.IPet,
                        global::TestCase.PetDto>(
                        polymorphicSource,
                        destination,
                        context);
                    return true;

                case { } when source is global::TestCase.IWorking ||
                    source is global::TestCase.IPet:
                    throw global::Morphant.Exceptions.AmbiguousPolymorphicMappingException.Create<
                        global::TestCase.IRoot,
                        global::TestCase.RootDto>(
                        global::Morphant.Context.MappingOperation.Update,
                        source,
                        (
                            source is global::TestCase.IWorking,
                            typeof(global::TestCase.IWorking),
                            typeof(global::TestCase.WorkingDto)),
                        (
                            source is global::TestCase.IPet,
                            typeof(global::TestCase.IPet),
                            typeof(global::TestCase.PetDto)));

                default:
                    result = default!;
                    return false;
            }
        }

        private global::TestCase.RootDto __Create()
        {
            return new global::TestCase.RootDto();
        }

        private global::TestCase.RootDto __Update(
            global::TestCase.RootDto destination)
        {
            return destination;
        }
    }
}
""";

        await ConventionTypeMapperGeneratorTest.RunAndAssert(
            LanguageVersion.CSharp9,
            source,
            expected);
    }

    [Test]
    public async Task Omits_destination_switch_for_base_destination_branch()
    {
        // lang=c#
        const string source =
"""
#nullable enable
#pragma warning disable CS1591

using Morphant;

namespace TestCase
{
    public class Animal { }
    public sealed class Dog : Animal { }
    public class AnimalDto { }

    [MorphantMapper]
    public partial class TestMapper : TypeMapper<TestMapper>
    {
        protected override void Configure(MapperBuilder builder)
        {
            builder.Map<Animal, AnimalDto>()
                .ForDerived<Dog, AnimalDto>();
        }
    }
}
""";

        // lang=c#
        const string expected =
"""
// <auto-generated />
#nullable enable

namespace TestCase
{
    public partial class TestMapper :
        global::Morphant.ITypeMapper<global::TestCase.Animal, global::TestCase.AnimalDto>
    {
        /// <inheritdoc/>
        protected override bool Supports(
            global::System.Type sourceType,
            global::System.Type destinationType) =>
                (sourceType == typeof(global::TestCase.Animal) &&
                    destinationType == typeof(global::TestCase.AnimalDto)) ||
                base.Supports(sourceType, destinationType);

        /// <inheritdoc/>
        global::TestCase.AnimalDto global::Morphant.ITypeMapper<global::TestCase.Animal, global::TestCase.AnimalDto>.Create(
            global::TestCase.Animal? source,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (__TryCreatePolymorphic(source, context, out var polymorphicDestination))
            {
                return polymorphicDestination;
            }

            return __Create(source, context);
        }

        /// <inheritdoc/>
        global::TestCase.AnimalDto global::Morphant.ITypeMapper<global::TestCase.Animal, global::TestCase.AnimalDto>.Update(
            global::TestCase.Animal? source,
            global::TestCase.AnimalDto? destination,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (__TryUpdatePolymorphic(source, destination, context, out var polymorphicDestination))
            {
                return polymorphicDestination;
            }

            if (destination is null)
            {
                return __Create(source, context);
            }

            return __Update(source, destination, context);
        }

        private bool __TryCreatePolymorphic(
            global::TestCase.Animal source,
            global::Morphant.Context.MappingContext context,
            out global::TestCase.AnimalDto result)
        {
            switch (source)
            {
                case global::TestCase.Dog polymorphicSource:
                    result = context.Mapper.Map<global::TestCase.Dog, global::TestCase.AnimalDto>(polymorphicSource);
                    return true;

                default:
                    result = default!;
                    return false;
            }
        }

        private bool __TryUpdatePolymorphic(
            global::TestCase.Animal source,
            global::TestCase.AnimalDto? destination,
            global::Morphant.Context.MappingContext context,
            out global::TestCase.AnimalDto result)
        {
            switch (source)
            {
                case global::TestCase.Dog polymorphicSource:
                    result = context.Mapper.Map<global::TestCase.Dog, global::TestCase.AnimalDto>(polymorphicSource, destination);
                    return true;

                default:
                    result = default!;
                    return false;
            }
        }

        private global::TestCase.AnimalDto __Create()
        {
            return new global::TestCase.AnimalDto();
        }

        private global::TestCase.AnimalDto __Update(
            global::TestCase.AnimalDto destination)
        {
            return destination;
        }
    }
}
""";

        await ConventionTypeMapperGeneratorTest.RunAndAssert(
            LanguageVersion.CSharp9,
            source,
            expected);
    }

    [Test]
    public async Task Uses_underlying_value_for_nullable_strict_source()
    {
        // lang=c#
        const string source =
"""
#nullable enable
#pragma warning disable CS1591

using Morphant;

namespace TestCase
{
    public readonly struct Number
    {
        public Number(int value) => Value = value;
        public int Value { get; }
    }

    public sealed class NumberDto
    {
        public int Value { get; set; }
    }

    [MorphantMapper]
    public partial class TestMapper : TypeMapper<TestMapper>
    {
        protected override void Configure(MapperBuilder builder)
        {
            builder.Map<Number?, NumberDto>()
                .UnknownDerivedTypeHandling(
                    UnknownDerivedTypeHandling.Throw);
        }
    }
}
""";

        // lang=c#
        const string expected =
"""
// <auto-generated />
#nullable enable

namespace TestCase
{
    public partial class TestMapper :
        global::Morphant.ITypeMapper<global::TestCase.Number?, global::TestCase.NumberDto>
    {
        /// <inheritdoc/>
        protected override bool Supports(
            global::System.Type sourceType,
            global::System.Type destinationType) =>
                (sourceType == typeof(global::TestCase.Number?) &&
                    destinationType == typeof(global::TestCase.NumberDto)) ||
                base.Supports(sourceType, destinationType);

        /// <inheritdoc/>
        global::TestCase.NumberDto global::Morphant.ITypeMapper<global::TestCase.Number?, global::TestCase.NumberDto>.Create(
            global::TestCase.Number? source,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            var sourceValue = source.Value;

            return __Create(sourceValue);
        }

        /// <inheritdoc/>
        global::TestCase.NumberDto global::Morphant.ITypeMapper<global::TestCase.Number?, global::TestCase.NumberDto>.Update(
            global::TestCase.Number? source,
            global::TestCase.NumberDto? destination,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            var sourceValue = source.Value;

            if (destination is null)
            {
                return __Create(sourceValue);
            }

            return __Update(sourceValue, destination, context);
        }

        private global::TestCase.NumberDto __Create(
            global::TestCase.Number sourceValue)
        {
            return new global::TestCase.NumberDto()
            {
                Value = sourceValue.Value
            };
        }

        private global::TestCase.NumberDto __Update(
            global::TestCase.Number sourceValue,
            global::TestCase.NumberDto destination)
        {
            destination.Value = sourceValue.Value;

            return destination;
        }
    }
}
""";

        await ConventionTypeMapperGeneratorTest.RunAndAssert(
            LanguageVersion.CSharp9,
            source,
            expected);
    }

    [Test]
    public async Task Uses_exact_runtime_type_for_sealed_source()
    {
        // lang=c#
        const string source =
"""
#nullable enable
#pragma warning disable CS1591

using Morphant;

namespace TestCase
{
    public sealed class Number
    {
        public Number(int value) => Value = value;
        public int Value { get; }
    }

    public sealed class NumberDto
    {
        public int Value { get; set; }
    }

    [MorphantMapper]
    public partial class TestMapper : TypeMapper<TestMapper>
    {
        protected override void Configure(MapperBuilder builder)
        {
            builder.Map<Number?, NumberDto>()
                .UnknownDerivedTypeHandling(
                    UnknownDerivedTypeHandling.Throw);
        }
    }
}
""";
        // lang=c#
        const string expected =
"""
// <auto-generated />
#nullable enable

namespace TestCase
{
    public partial class TestMapper :
        global::Morphant.ITypeMapper<global::TestCase.Number?, global::TestCase.NumberDto>
    {
        /// <inheritdoc/>
        protected override bool Supports(
            global::System.Type sourceType,
            global::System.Type destinationType) =>
                (sourceType == typeof(global::TestCase.Number) &&
                    destinationType == typeof(global::TestCase.NumberDto)) ||
                base.Supports(sourceType, destinationType);

        /// <inheritdoc/>
        global::TestCase.NumberDto global::Morphant.ITypeMapper<global::TestCase.Number?, global::TestCase.NumberDto>.Create(
            global::TestCase.Number? source,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            return __Create(source, context);
        }

        /// <inheritdoc/>
        global::TestCase.NumberDto global::Morphant.ITypeMapper<global::TestCase.Number?, global::TestCase.NumberDto>.Update(
            global::TestCase.Number? source,
            global::TestCase.NumberDto? destination,
            global::Morphant.Context.MappingContext context)
        {
            if (source is null)
            {
                return default!;
            }

            if (destination is null)
            {
                return __Create(source, context);
            }

            return __Update(source, destination, context);
        }

        private global::TestCase.NumberDto __Create(
            global::TestCase.Number source)
        {
            return new global::TestCase.NumberDto()
            {
                Value = source.Value
            };
        }

        private global::TestCase.NumberDto __Update(
            global::TestCase.Number source,
            global::TestCase.NumberDto destination)
        {
            destination.Value = source.Value;

            return destination;
        }
    }
}
""";
        await ConventionTypeMapperGeneratorTest.RunAndAssert(LanguageVersion.CSharp9, source, expected);
    }

    [Test]
    public async Task Retains_strict_dispatch_for_covariant_arrays()
    {
        // lang=c#
        const string source =
"""
#nullable enable
#pragma warning disable CS1591
using Morphant;
namespace TestCase
{
    [MorphantMapper]
    public partial class TestMapper : TypeMapper<TestMapper>
    {
        protected override void Configure(MapperBuilder builder) =>
            builder.Map<object[], int>()
                .UnknownDerivedTypeHandling(UnknownDerivedTypeHandling.Throw)
                .Convert(source => source!.Length);
    }
}
""";
        // lang=c#
        const string expected =
"""
// <auto-generated />
#nullable enable

namespace TestCase
{
    public partial class TestMapper :
        global::Morphant.ITypeMapper<object[], int>
    {
        /// <inheritdoc/>
        protected override bool Supports(
            global::System.Type sourceType,
            global::System.Type destinationType) =>
                (sourceType == typeof(object[]) &&
                    destinationType == typeof(int)) ||
                base.Supports(sourceType, destinationType);

        /// <inheritdoc/>
        int global::Morphant.ITypeMapper<object[], int>.Create(
            object[]? source,
            global::Morphant.Context.MappingContext context)
        {
            if (__TryCreatePolymorphic(source, out var polymorphicDestination))
            {
                return polymorphicDestination;
            }

            return __ConvertDestination(source);
        }

        /// <inheritdoc/>
        int global::Morphant.ITypeMapper<object[], int>.Update(
            object[]? source,
            int destination,
            global::Morphant.Context.MappingContext context)
        {
            if (__TryUpdatePolymorphic(source, out var polymorphicDestination))
            {
                return polymorphicDestination;
            }

            return __ConvertDestination(source);
        }

        private bool __TryCreatePolymorphic(
            object[]? source,
            out int result)
        {
            switch (source)
            {
                case { } when source.GetType() !=
                    typeof(object[]):
                    throw global::Morphant.Exceptions.UnmatchedPolymorphicMappingException.Create<
                        object[],
                        int>(
                        global::Morphant.Context.MappingOperation.Create,
                        source);

                default:
                    result = default!;
                    return false;
            }
        }

        private bool __TryUpdatePolymorphic(
            object[]? source,
            out int result)
        {
            switch (source)
            {
                case { } when source.GetType() !=
                    typeof(object[]):
                    throw global::Morphant.Exceptions.UnmatchedPolymorphicMappingException.Create<
                        object[],
                        int>(
                        global::Morphant.Context.MappingOperation.Update,
                        source);

                default:
                    result = default!;
                    return false;
            }
        }

        private int __ConvertDestination(object[]? source) => source!.Length;
    }
}
""";
        await ProductionGeneratorTest.RunAndAssert(LanguageVersion.CSharp9, source,
            ("Morphant.Generated.TypeMapper.TestMapper__3257fb115d47a54e47a0d30bb8ee81d3.g.cs", expected),
            ("Morphant.Generated.MappingExtension.TestMapper.ObjectArray1ToInt32__490d4386cef1cde2342404804816bc44.g.cs",
            // lang=c#
"""
// <auto-generated />
#nullable enable

namespace Morphant
{
    internal static partial class MorphantGeneratedMappingExtensions
    {
        /// <summary>
        /// Creates a destination through a callback only when none exists.
        /// </summary>
        /// <param name="builder">The mapping to configure.</param>
        /// <param name="construct">Callback returning the destination; null ends the mapping.</param>
        /// <seealso href="https://github.com/strangeman375/Morphant/blob/main/docs/api/construct-using.md"/>
        public static global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> ConstructUsing(
            this global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> builder,
            global::Morphant.Delegates.ConstructUsing<object[], int> construct)
            => throw new global::Morphant.Exceptions.RuntimeInvocationNotSupportedException();

        /// <summary>
        /// Creates a destination through a callback only when none exists.
        /// </summary>
        /// <param name="builder">The mapping to configure.</param>
        /// <param name="construct">Callback returning the destination; null ends the mapping.</param>
        /// <seealso href="https://github.com/strangeman375/Morphant/blob/main/docs/api/construct-using.md"/>
        public static global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> ConstructUsing(
            this global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> builder,
            global::Morphant.Delegates.ConstructUsing<object[], global::Morphant.Context.MappingContext, int> construct)
            => throw new global::Morphant.Exceptions.RuntimeInvocationNotSupportedException();

        /// <summary>
        /// Chooses the destination through a callback on Create and Update.
        /// </summary>
        /// <param name="builder">The mapping to configure.</param>
        /// <param name="resolve">Callback returning the destination; null ends the mapping.</param>
        /// <seealso href="https://github.com/strangeman375/Morphant/blob/main/docs/api/resolve-using.md"/>
        public static global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> ResolveUsing(
            this global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> builder,
            global::Morphant.Delegates.ResolveUsing<object[], int, int> resolve)
            => throw new global::Morphant.Exceptions.RuntimeInvocationNotSupportedException();

        /// <summary>
        /// Chooses the destination through a callback on Create and Update.
        /// </summary>
        /// <param name="builder">The mapping to configure.</param>
        /// <param name="resolve">Callback returning the destination; null ends the mapping.</param>
        /// <seealso href="https://github.com/strangeman375/Morphant/blob/main/docs/api/resolve-using.md"/>
        public static global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> ResolveUsing(
            this global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> builder,
            global::Morphant.Delegates.ResolveUsing<object[], int, global::Morphant.Context.MappingContext, int> resolve)
            => throw new global::Morphant.Exceptions.RuntimeInvocationNotSupportedException();

        /// <summary>
        /// Maps with ordinary C#, bypassing null policies and member rules.
        /// </summary>
        /// <param name="builder">The mapping to configure.</param>
        /// <param name="mapping">Lambda, method group, or delegate returning the final result.</param>
        /// <seealso href="https://github.com/strangeman375/Morphant/blob/main/docs/api/convert.md"/>
        public static global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> Convert(
            this global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> builder,
            global::Morphant.Delegates.Convert<object[]?, int> mapping)
            => throw new global::Morphant.Exceptions.RuntimeInvocationNotSupportedException();

        /// <summary>
        /// Maps with ordinary C#, bypassing null policies and member rules.
        /// </summary>
        /// <param name="builder">The mapping to configure.</param>
        /// <param name="mapping">Lambda, method group, or delegate returning the final result.</param>
        /// <seealso href="https://github.com/strangeman375/Morphant/blob/main/docs/api/convert.md"/>
        public static global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> Convert(
            this global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> builder,
            global::Morphant.Delegates.Convert<object[]?, int, int> mapping)
            => throw new global::Morphant.Exceptions.RuntimeInvocationNotSupportedException();

        /// <summary>
        /// Maps with ordinary C#, bypassing null policies and member rules.
        /// </summary>
        /// <param name="builder">The mapping to configure.</param>
        /// <param name="mapping">Lambda, method group, or delegate returning the final result.</param>
        /// <seealso href="https://github.com/strangeman375/Morphant/blob/main/docs/api/convert.md"/>
        public static global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> Convert(
            this global::Morphant.MappingBuilder<global::TestCase.TestMapper, object[], int> builder,
            global::Morphant.Delegates.Convert<object[]?, int, global::Morphant.Context.MappingContext, int> mapping)
            => throw new global::Morphant.Exceptions.RuntimeInvocationNotSupportedException();
    }
}
"""));
    }

}
